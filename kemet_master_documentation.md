# 📜 وثيقة المرجع التقني والتاريخي الشامل لمنصة KEMET Sportswear

> **تاريخ التوثيق:** أغسطس 2026  
> **العلامة التجارية:** KEMET — Build Your Legacy 🇪🇬  
> **النطاق الرسمي:** `kemetmisr.com`  
> **حالة المشروع:** مكتمل وجاهز للعمل والإنتاج بنسبة 100% (Production Ready)  

---

## 🏛️ 1. الرؤية المعمارية والتقنية (Tech Stack & Architecture)

تم بناء منصة **KEMET** وفق أحدث المعايير الهندسية لتطبيقات الويب العالمية لتوفير أداء فائق السرعة، تجربة مستخدم ملكية فاخرة، وأمان مشدد:

| الطبقة (Layer) | التقنية المستخدمة | الوصف والدور المعماري |
| :--- | :--- | :--- |
| **إطار العمل الأساسي (Framework)** | **Next.js 14 (App Router)** | معمارية `React Server Components` مع `Server Actions` لتحقيق أعلى سرعة تحميل وأفضل نتائج لمحركات البحث (SEO). |
| **لغة الواجهات والمنطق** | **React 18 + JavaScript (ESNext)** | إدارة الحالة عبر `AppContext` الموحد مع التخزين المحلي `LocalStorage` للمزامنة الفورية. |
| **نظام التصميم والأناقة (Design System)** | **Pure Vanilla CSS & CSS Variables** | تصميم فاخر بهوية فرعونية ملكية حديثة تدعم الوضع الداكن والوضع الفاتح (`Dark & Light Themes`) مع تأثيرات `Glassmorphism` و `Golden Glow`. |
| **قاعدة البيانات والتخزين (Database & Storage)** | **Supabase (PostgreSQL 15)** | قاعدة بيانات سحابية متقدمة مع `Row Level Security (RLS)` و `Supabase Storage` لرفع الصور بصيغ `.webp` فائقة الضغط والسرعة. |
| **نظام البريد الإلكتروني والمراسلات** | **Resend REST API** | خدمة بريد سحابية رسمية مع قوالب `HTML` مخصصة ومطابقة لهوية البراند عبر البريد الرسمي `KEMET <support@kemetmisr.com>`. |
| **الاستضافة والبنية التحتية** | **Vercel Edge Network** | نشر عالمي واستجابة فائقة السرعة من أقرب خوادم للشرق الأوسط ومصر مع شهادات أمان `SSL / TLS` مفعلة تلقائياً. |

---

## 💎 2. الميزات والأنظمة المنجزة للعميل (Customer Experience & Core Features)

### 🌟 الصفحة الرئيسية والواجهة (Homepage & Dynamic Rails):
- **الهيرو سكشن الملكي (Hero Banner)**: شعار KEMET الذهبي المتحرك، أزرار توجيه سريعة، وبانر إعلاني ديناميكي للشحن المجاني والعروض قابل للتحكم من لوحة الإدارة.
- **سلايدرات المنتجات الأفقية المحدثة (Curva-Style Rails)**:
  - كروت عريضة ومتناسقة بنسبة أبعاد عالمية (`255px / 68vw`) تمنح صور المنتجات مساحة عرض فخمة وواضحة.
  - هوامش جانبية ضيقة تستغل الشاشة بالكامل على الهواتف.
  - هيدر أنيق ومباشر بعنوان السلايدر وزر `عرض الكل ←`.
- **أقسام فئات المنتجات التفاعلية (Interactive Categories)**: أطقم وتيشيرتات رسمية، ملابس تمرين وجيم، وشورتات ومستلزمات رياضية.
- **قسم لماذا KEMET (Why KEMET)**: إبراز مميزات البراند (خامات قطنية معالجة ضد التعرق، توصيل سريع لجميع المحافظات، ومعاينة واستبدال سهل).

---

### 🛍️ كروت المنتجات وتفاصيل المنتجات (Product Card & Details Page):
- **معاينة المقاسات الحية (Live Database Sizes)**:
  - ربط مباشر بمخزون جدول `product_variants` في Supabase.
  - المقاسات المنتهية تظهر مشطوبة باللون الأحمر وتكون غير قابلة للشراء لحماية المخزون.
- **نافذة تكبير وفحص الصور الاحترافية (ImageZoom via React Portal)**:
  - تكبير تفاعلي مباشر باللمس أو الماوس.
  - نافذة مكبرة تغطي الشاشة بالكامل بنظام `React Portal` متجاوزة أي قيود طبقات، وتغطي النافبار والبار السفلي مع شريط أدوات تحكم زجاجي فاخر (`+`, `-`, `ضبط 🔄`, `✕`) أعلى الشاشة.
- **معرض الصور المصغرة (Image Gallery Carousel)**: استعراض جميع زوايا القطعة بضغطة زر.

---

### 🛒 حقيبة التسوق الذكية (Cart Drawer & Size Modifier):
- **توافق 100% مع الوضعين الفاتح والداكن**: تصميم واسع ومريح للعين مع بطاقات بيضاء فاخرة في اللايت مود وخلفيات ليلية ذهبية في الدارك مود.
- **تعديل المقاس داخل السلة مباشرة (On-the-fly Size Selector)**: يستطيع العميل تغيير مقاس أي قطعة داخل السلة من بين المقاسات المتوفرة في قاعدة البيانات فوراً.
- **تعديل الكميات والحذف الفوري**: أزرار `+` و `-` مريحة وزر `✕ حذف` لإلغاء أي قطعة دون الحاجة لإتمام الشراء.
- **حساب الإجمالي التلقائي**: تحديث سعر المنتجات ومصاريف الشحن فوراً.

---

### 💳 إتمام الطلب والكوبونات (Checkout & Discount Engine):
- **محرك الكوبونات والبروموكود (Promocode Engine)**:
  - دعم الخصم المئوي (مثل `10%`) والخصم الثابت (مثل `50 ج.م`).
  - حدود الاستخدام الإجمالية والحد الأقصى لكل عميل لمنع إساءة الاستخدام.
- **حساب الشحن الديناميكي بالمحافظات**: مصاريف شحن مخصصة لـ 27 محافظة مع دعم الشحن المجاني التلقائي في حالة تفعيل العرض.
- **شاشة تأكيد الطلب الملكية (Success Confirmation Screen)**:
  - عرض رقم الطلب المميز (مثل `#KM-2027-xxxx`).
  - إظهار الإجمالي الكلي بالأرقام الواضحة ومبلغ الخصم والعنوان.
  - روابط سريعة لـ "متابعة طلباتي" و "العودة للمتجر".

---

### 🔐 حسابات العملاء وتسجيل الدخول (Customer Accounts & OTP):
- **نظام تسجيل الدخول بدون كلمة سر (Magic Link / Passwordless)**: إرسال كود تحقق فوري عبر البريد الإلكتروني.
- **صفحة طلباتي وإدارة الحساب (`/my-orders`)**:
  - عرض كافة طلبات العميل الحالية والسابقة وتفاصيلها.
  - إمكانية تعديل عنوان التوصيل والمقاسات أو إلغاء الطلب طالما كان في مرحلة "جديد" أو "جاري التجهيز".
  - تحديث بيانات الاسم، الهاتف، المحافظة، والعنوان الشخصي.
  - زر محادثة واتساب مباشر مسبق التنسيق برقم الأوردر لأي استفسار.

---

## 🎛️ 3. لوحة التحكم والإدارة الشاملة (Admin Control Panel)

لوحة تحكم إدارية محمية مخصصة لإدارة المتجر بالكامل دون الحاجة لأي خبرة برمجية:

1. **إدارة المنتجات والمخزون (Products & Inventory)**:
   - إضافة وتعديل وحذف المنتجات ورفع الصور المتعددة وتوليد روابط `.webp` سريعة.
   - تحديد مقاسات كل منتج والكمية المتوفرة لكل مقاس (`Stock Management`).
   - تمييز المنتجات كـ `الأكثر مبيعاً 🔥`، `جديد ⚡`، أو `Premium Gold 👑`.
   - خيار إخفاء/تفعيل المنتجات في المتجر بضغطة زر.

2. **إدارة الطلبات والحالات ومندوبي الشحن (Orders Management)**:
   - جدول تفصيلي بجميع الطلبات الواردة مع فلترة فورية بالحالة والاسم ورقم الموبايل.
   - تغيير حالة الأوردر (`جديد 📦` -> `جاري التجهيز ⚙️` -> `تم الشحن 🚚` -> `مع المندوب 🛵` -> `تم التسليم ✅` -> `ملغي ❌`).
   - إضافة كود تتبع الشحنة لدى البريد المصري / شركة الشحن.
   - إرسال إيميل رسمي تلقائي وفوري للعميل عند تغيير أي حالة يوضح التحديث ورقم التتبع.

3. **إرسال رسائل خاصة ومباشرة للعملاء (Direct Customer Messaging)**:
   - نافذة مخصصة لإرسال إيميل خاص لعميل أي أوردر مباشرة من لوحة التحكم بتصميم وهوية KEMET الرسمية وباسم `KEMET <support@kemetmisr.com>`.

4. **الحملات البريدية الترويجية (Mass Promo Campaigns)**:
   - إمكانية إرسال إيميل جماعي لجميع المسجلين في المتجر بضغطة زر للإعلان عن عروض أو تشكيلات جديدة.

5. **التحكم في أقسام وسلايدرات الصفحة الرئيسية (Homepage Sections CMS)**:
   - تغيير أسماء وعناوين السلايدرات بالعربي والإنجليزي (مثل تحويلها إلى "أفضل المنتجات 🔥").
   - ترتيب الأقسام لأعلى ولأسفل، وتحديد عدد المنتجات المعروضة في كل سلايدر.

6. **التحكم في البانر الإعلاني العلوي (Promo Banner CMS)**:
   - تعديل نص البانر وإظهاره أو إخفائه فورياً من قاعدة البيانات لجميع الزوار.

---

## 🛡️ 4. الأمان وحماية البيانات (Security & Best Practices)

- **حماية المفاتيح السرية (Zero Secret Exposure)**:
  - مفتاح `SUPABASE_SERVICE_ROLE_KEY` ومفتاح `RESEND_API_KEY` محصوران تماماً داخل بيئة السيرفر (`Node.js Server Actions`) ولا يتسرب أي منهما إلى متصفح العميل إطلاقاً.
- **قواعد أمان قاعدة البيانات (Row Level Security - RLS)**:
  - الجداول محمية بحيث لا يستطيع أي زائر قراءة أو تعديل بيانات عملاء آخرين أو تغيير أسعار المنتجات.
- **التشفير والاتصال الآمن (HTTPS & SSL)**:
  - جميع الاتصالات بين المتصفح والسيرفر وقاعدة البيانات مشفرة بالكامل.
- **عزل لوحة التحكم الإدارية (Admin Isolation)**:
  - لوحة التحكم مفصولة ومحمية بالتحقق من الهوية والأدوار الإدارية (`Admin Roles`).

---

## 📈 5. ما تم إنجازه وما يمكن إضافته مستقبلاً (Roadmap & Recommendations)

### ✅ ما تم إنجازه بنسبة 100%:
1. متجر إلكتروني متكامل للعلامة التجارية الفاخرة KEMET.
2. دعم كامل للغتين (العربية والإنجليزية) والوضعين (الفاتح والداكن).
3. سلة تسوق ديناميكية بتعديل المقاسات والكميات.
4. نظام دفع عند الاستلام (COD) مع حساب دقيق لمصاريف الشحن لكل محافظة.
5. نظام كوبونات وعروض ترويجية وبانر إعلاني ديناميكي.
6. إدارة متقدمة للمخزون والمقاسات منتهية الكمية.
7. تكبير احترافي لصور المنتجات بدقة عالية بنظام React Portal.
8. نظام إيميلات رسمي موحد وموثق عبر Resend API (`support@kemetmisr.com`).
9. لوحة تحكم إدارية شاملة للمنتجات، الأقسام، الطلبات، والرسائل.
10. تصميم متجاوب فائق الخفة والسرعة للهواتف والأجهزة اللوحية والكمبيوتر.

### 🚀 مقترحات للتطوير المستقبلي (عند الرغبة في التوسع):
- **بوابات الدفع الإلكتروني (Online Payment Gateway)**: ربط بوابات مثل Paymob أو Fawry أو Visa/Mastercard للدفع المسبق بجانب الدفع عند الاستلام.
- **الربط الآلي مع شركات الشحن (Courier API Integration)**: ربط واجهات برمجة التطبيقات مع شركات مثل Bosta أو Mylerz لإنشاء بوالص الشحن بضغطة زر.
- **تحويل المتجر إلى تطبيق هاتف (PWA / Mobile App)**: تحويل الكود الحالي إلى تطبيق موبايل لأجهزة Android و iOS عبر تقنية Capacitor أو React Native.
- **لوحة تحليلات وتقارير أرباح متقدمة (Advanced Sales Analytics)**: رسوم بيانية لحجم المبيعات اليومية والأرباح الصافية ومعدلات نمو العملاء.

---

> **🏆 خلاصة التوثيق:**  
> تم بناء وتطوير منصة **KEMET Sportswear** بمستوى هندسي وتصميمي ينافس أكبر البراندات الرياضية العالمية، وهي الآن في قمة استقرارها وجاهزيتها الكاملة لاستقبال العملاء وتحقيق أعلى معدلات المبيعات والنجاح بإذن الله! 🚀
